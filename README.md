# Ферма «Ново-Простоквашино»

## Основной интерфейс
Окно разделено на две части:

**Левая панель:**
- Выбор формата для импорта (Пример данных, JSON, CSV, TXT)
- 📁 Кнопка загрузки файла с животными
- Список животных, находящихся на ферме
- Кнопки "В очередь" (обычное кормление), 🚨 "Срочно!" (приоритет), "Очистить"

**Правая панель:**
- Дерево таксономической иерархии выбранного животного (Тип → Класс → Отряд → Семейство → Род → Вид → Животное)
- Кнопка "Назад" для возврата к предыдущему животному (работает Stack)
- История просмотра с количеством посещений
- Очередь кормления (Deque) с приоритетом: обычные животные → в конец, срочные → в начало (➡️ в очереди, ⏳ ожидающие)
- Кнопка "Накормить следующего" с анимацией прогресс-бара

### Типичный сценарий использования

1. 📁 **Загрузка данных**: выбираем "Пример данных" или импортируем JSON/CSV/TXT файл
2. **Просмотр животных**: кликаем на животное в списке → видим полную иерархию (от Типа Хордовые до конкретного животного)
3. **История просмотров**: история автоматически ведётся (Stack LIFO). Нажимаем "Назад" → возвращаемся к предыдущему просмотренному животному
4. **Кормление**: добавляем животных в очередь обычным способом или срочным (они встанут в начало)
5. **Кормим**: нажимаем "Накормить" → берётся первое из очереди, полоса заполняется


## Реализованные концепции ООП

### 1. Наследование (7 уровней)

Все таксономические ранги наследуются от базового класса `TaxonomicRank`:

```
TaxonomicRank (ABC - абстрактный базовый класс)
    ├── Phylum (Тип) — самый общий уровень
    ├── ClassAnimal (Класс)
    ├── Order (Отряд)
    ├── Family (Семейство)
    ├── Genus (Род)
    ├── Species (Вид)
    └── Animal (Животное) — последний уровень (конкретное животное)
```

Каждый подкласс:
- Переопределяет абстрактные методы `get_parent()` и `get_rank_name()`
- Добавляет свои атрибуты через агрегацию

### 2. Агрегация vs Наследование

Проект использует **оба подхода одновременно:**

- **Наследование (IS-A)**: Species **является** TaxonomicRank, переопределяет методы
- **Агрегация (HAS-A)**: Animal **содержит** Species, Species **содержит** Genus и т.д.

**Почему агрегация вместо наследования?**

- Animal не IS-A Species. Животное **принадлежит** виду, не является видом
- Одно животное не может быть несколько видов (наследование - один родитель)
- Нельзя переиспользовать: две кошки должны ссылаться на один объект Species

**Пример:**
```python
cat_species = Species("Домашняя кошка", cat_genus)
murka = Animal("Мурка", cat_species, 3, 4.5, "рыжая")
murzik = Animal("Мурзик", cat_species, 2, 4.2, "чёрный")
# Две кошки, один Species объект
```

---

Каждый уровень содержит ссылку на родительский:

```python
Animal(name="Мурка") 
  → Species(name="Домашняя кошка")
    → Genus(name="Кошки")
      → Family(name="Кошачьи")
        → Order(name="Хищные")
          → ClassAnimal(name="Млекопитающие")
            → Phylum(name="Хордовые")
```

### 3. Инкапсуляция (защита данных)

**На уровне объекта:**
- `_name` (protected) — доступна наследникам через `@property`
- `__age`, `__weight` (private) — только внутри класса через `@property`
- Валидация через сеттеры:
  ```python
  @age.setter
  def age(self, value):
      if value < 0:
          raise ValueError("Возраст не может быть отрицательным")
      self.__age = value
  ```

**На уровне класса:**
- `__id_counter` (private атрибут класса) — скрыт полностью
- Доступ только через `@classmethod`:
  ```python
  @classmethod
  def get_count(cls):
      return cls.__id_counter
  ```

### 4. Полиморфизм

Каждый подкласс переопределяет методы интерфейса:
```python
class Animal(TaxonomicRank):
    def get_rank_name(self):
        return "Животное"

class Species(TaxonomicRank):
    def get_rank_name(self):
        return "Вид"
```

## Структуры данных

### 1. Stack (LIFO) — История просмотров

**Где используется:** Кнопка "Назад" в правой панели интерфейса

**Как работает:**
- При клике на животное: `stack.push(animal.name)`
- При клике "Назад": `stack.pop()` → возвращаемся к предыдущему

**Пример:**
```
Пользователь кликает: Мурка → Матроскин → Шарик
Stack содержит: [Мурка, Матроскин, Шарик]
Клик "Назад": pop() → возвращается на Матроскина
Stack: [Мурка, Матроскин]
```

**Методы:**
- `push(item)` — добавить на вершину
- `pop()` — извлечь с вершины
- `peek()` — посмотреть вершину без удаления
- `is_empty()` — пуст ли стек
- `size()` — количество элементов
- `clear()` — очистить стек

### 2. Deque (двусторонняя очередь) — Приоритетное кормление

**Где используется:** Очередь кормления в правой панели интерфейса

**Как работает:**
- "В очередь" → `deque.push_back()` (добавить в конец)
- "Срочно!" → `deque.push_front()` (добавить в начало)
- "Накормить" → `deque.pop_front()` (берём первого)

**Пример:**
```
Очередь: [➡️ Мурка, ⏳ Матроскин, ⏳ Шарик]
Добавляем 🚨 "Срочно! Бобик" → push_front()
Очередь: [➡️ 🚨 Бобик, ⏳ Мурка, ⏳ Матроскин, ⏳ Шарик]
Кормим → pop_front() удаляет Бобика
Очередь: [➡️ Мурка, ⏳ Матроскин, ⏳ Шарик]
```

**Методы:**
- `push_front(item)` — добавить в начало
- `push_back(item)` — добавить в конец
- `pop_front()` — извлечь с начала
- `is_empty()` — пуста ли очередь
- `size()` — размер очереди


## 📁 Паттерн Мост (Bridge)

**Проблема:** Есть данные (животные) и форматы хранения (JSON, CSV, TXT). Если создавать класс для каждой комбинации, получим комбинаторный взрыв.

**Решение:** Разделить на две независимые иерархии:
1. **Абстракция** — что экспортировать (интерфейс `ExportFormat`)
2. **Реализация** — как экспортировать (JSON, CSV, TXT, ...) 

**Структура кода:**

```python
# Абстракция (интерфейс)
class ExportFormat(ABC):
    def export(data, filepath): ...
    def import_data(filepath): ...
    def get_extension(): ...
    def get_name(): ...

# Конкретные реализации
class JsonFormat(ExportFormat): ...  # JSON
class CsvFormat(ExportFormat): ...   # CSV
class TxtFormat(ExportFormat): ...   # TXT
```

**Как используется в приложении:**

```python
format_name = "JSON"
fmt = JsonFormat()  # Выбираем реализацию
data = fmt.import_data(filepath)  # Используем единый интерфейс
```

**Преимущества:**
- Легко добавить новый формат (новый класс)
- Клиентский код не меняется
- 3 формата + 1 интерфейс = 4 класса вместо N×3


## Примеры данных

### 🐾 Созданные животные (при загрузке "Пример данных"):

```
🐄 Мурка - Домашняя корова (5 лет, 450 кг)
🐱 Матроскин - Домашняя кошка (3 года, 4.5 кг)
🐕 Шарик - Домашняя собака (4 года, 15 кг)
🐔 Пеструшка - Домашняя курица (2 года, 2 кг)
```

### Полная иерархия для Мурки:

```
Тип: Хордовые
├─ Класс: Млекопитающие
   ├─ Отряд: Парнокопытные
      ├─ Семейство: Полорогие
         ├─ Род: Быки
            ├─ Вид: Домашняя корова
               └─ Животное: Мурка (5 лет, 450 кг)
```

## 📁 Импорт данных

Программа поддерживает импорт животных в трёх форматах:
- **JSON** — универсальный формат, легко редактировать
- **CSV** — табличный формат, совместим с Excel
- **TXT** — простой текстовый формат

**Как использовать:**
1. Выбрать формат из выпадающего списка
2. Нажать 📁 "Загрузить"
3. Выбрать файл нужного формата
4. Животные появляются в списке в левой панели интерфейса


## Запуск

```bash
python main.py
```

**Требования:**
- Python 3.10+
- PyQt6

**Установка:**
```bash
pip install PyQt6
```


## Что демонстрирует проект

| Концепция | Где видно |
|-----------|-----------|
| **Наследование** | 7 классов Phylum→Animal наследуют TaxonomicRank |
| **Агрегация** | Animal содержит Species, Species содержит Genus и т.д. |
| **Инкапсуляция** | `__age`, `__weight` защищены, доступ через `@property` с валидацией |
| **Полиморфизм** | Все ранги переопределяют `get_rank_name()` и `get_parent()` |
| **Stack (LIFO)** | История просмотров — кнопка "Назад" |
| **Deque** | Приоритетное кормление — обычные в конец, срочные в начало |
| **Паттерн Мост** | JsonFormat, CsvFormat, TxtFormat с общим интерфейсом |
| **Фабричный метод** | `create_sample_animals()` создаёт полную иерархию |
